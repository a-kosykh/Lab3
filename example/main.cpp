// Вычислить сумму ряда:
// S = ∑(k=1)->inf = ((-1)^(k-1))/(k^3)
// С точностью e=10^(-4)
#include <iostream>
#include <math.h>
#include "func.hpp"
using namespace std;
int main() {
    cout << sum << endl;
    return 0;
}
